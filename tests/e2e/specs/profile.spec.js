describe('Tests about the profile page', () => {
  it('renders user page', () => { });
  it('shows user stats', () => { });
  it('updates user information, shows the updated user profile', () => {});
  it('renders list of user posts and tips', () => { });
  it('renders list of user pinned items', () => { });
});
