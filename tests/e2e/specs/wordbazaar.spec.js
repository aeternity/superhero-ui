describe('Test WordBazaar pages', () => {
  describe('Assets list', () => {
    it('renders list of assets available for buy and sell', () => {});
    it('searches in tokens names and renders only the results', () => {});
  });
  describe('Single token', () => {
    describe('Token info', () => {
      it('renders token information', () => {});
      it('can buy tokens', () => {});
      it('can sell tokens', () => {});
    });
    describe('Token vote', () => {
      it('shows active votes', () => {});
      it('can initiate a new voting', () => {});
      it('can add a vote', () => {});
      it('can revoke a vote', () => {});
    });
  });
});
